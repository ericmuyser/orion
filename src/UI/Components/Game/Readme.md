Basic game:

    var props = {
        width: 500,
        height: 500
    };

    <Game {...props} />

Tron game:

    var props = {
        width: 500,
        height: 500
    };

    <Game {...props} />